# 2026-06-12 中午任务执行记录

## 本次用户要求

1. `WFSampling.py` 改回原始思路并保存 `.npy`（不要 `.npz`），然后全量运行。  
2. `outputs/fea6/confusion_matrix.png` 改成显示比例小数。  
3. `audit/task7_plots/fea6/auc_vs_vise.png` 增加误差棒、取消连线。  
4. 修复 `fea6+decon_npevst` 与 `fea6+decon_waveform` smoke 问题。  
5. 训练时 `numu/nue/nc` 样本数保持一致（以 numu 为基准）。  
6. `decon_npevst` 改为稀疏点填充方式（类似 WFSampling），不再用 `(17612,200,1)` 分箱。  
7. `train_cnn_ds_pid.py`：CNN 输出改 6 维；DeepSphere 输入维度改到 `(None,12288,9)`。  
8. 新增并行任务 `fea6+waveform`。  
9. 同步更新 `WFSampling/CHANGES.md`、`README.md`。

## 已修改文件

- `pid_lib/waveform_io.py`
  - 新增 `load_waveform()`（原始 waveform）。
  - `load_decon_npevst()` 改为输出 `(N_PMT, max_points, 2)` + lengths（稀疏点填充）。
  - `load_wfsampling()` 支持优先读取新版 `.npy`，同时兼容旧 `.npz`。
  - `estimate_waveform_params()` 支持 `waveform`、新版 `decon_npevst` 与 `.npy` WFSampling。

- `WFSampling/WFSampling.py`
  - 输出格式从 `wfsampling_*.npz` 改为 `wfsampling_*.npy`（对象字典）。
  - 保留原有基线扣除和关键点提取算法。
  - 新 `.npy` 字段：`time`, `adc`, `offsets`, `n_events`, `file_id`, `n_pmt`, `empty_frac`。

- `WFSampling/show.py`
  - 读取优先 `wfsampling_*.npy`，若不存在再回退到旧 `*.npz`。

- `WFSampling/train_cnn_ds_pid.py`
  - `--waveform-source` 新增 `waveform`。
  - `decon_npevst` 改稀疏点输入 `(N_PMT,max_points,2)`。
  - CNN 输出维度从 4 改为 6。
  - 新增静态特征压缩层 `elec_proj3`（6→3），融合后 DeepSphere 输入为 9 维。
  - 模型结构确认含 `(None,12288,9)` 中间输出。
  - 数据集生成使用 `(inputs_tuple, label)`，修复 Keras 双输入错误。
  - 增加按类别事件数平衡（按最小类 cap）用于 train/val/test 流。

- `WFSampling/CHANGES.md`
  - 更新为四种波形任务，并同步记录 CNN=6、DeepSphere=9 和 `.npy` 格式变更。

- `tools/plot_pid_scores_energy.py`
  - visE 分箱 AUC 图改为“仅散点 + 误差棒”（无连线）。
  - 新增 bootstrap 误差估计并写入 `energy_auc_bins.json`。

- `README.md`
  - 更新四个 CNN+DeepSphere 输出目录说明。
  - 记录 WFSampling 主输出已切为 `.npy`。

## 已执行命令与结果

- 重新生成 `fea6` 混淆矩阵（比例小数）：
  - 运行 `evaluate_predictions(y_true, y_prob, outputs/fea6)`
  - 更新 `outputs/fea6/confusion_matrix.png`（行归一化小数显示）

- 重新绘制 task7 图：
  - 运行 `tools/plot_pid_scores_energy.py --model-dir outputs/fea6 ...`
  - 更新 `audit/task7_plots/fea6/auc_vs_vise.png`（误差棒散点）

- 停止旧 WFSampling 全量进程（旧 `.npz` 版）并重启新 `.npy` 版全量任务：
  - `logs/wfs_numu_full.pid`
  - `logs/wfs_nue_full.pid`
  - `logs/wfs_nc_full.pid`

- 重新启动 smoke：
  - `outputs/fea6+decon_npevst`（log: `logs/train_cnn_ds_decon_npevst_smoke.log`）
  - `outputs/fea6+decon_waveform`（log: `logs/train_cnn_ds_decon_waveform_smoke.log`）
  - `outputs/fea6+waveform`（log: `logs/train_cnn_ds_waveform_smoke.log`）

## 新增/更新的输出文件与目录

- 新增目录（若不存在）：
  - `outputs/fea6+waveform/`
- 更新图：
  - `outputs/fea6/confusion_matrix.png`
  - `audit/task7_plots/fea6/auc_vs_vise.png`
- 新增/更新日志：
  - `logs/train_cnn_ds_waveform_smoke.log`
  - `logs/train_cnn_ds_decon_npevst_smoke.log`
  - `logs/train_cnn_ds_decon_waveform_smoke.log`
  - `logs/wfs_numu_full.log`
  - `logs/wfs_nue_full.log`
  - `logs/wfs_nc_full.log`

## 当前状态

- 新版三类 WFSampling 全量（`.npy`）已在后台运行。  
- 三个 smoke（`decon_npevst` / `decon_waveform` / `waveform`）已重新启动，正在执行中。  
- `train_cnn_ds_pid.py` 的关键结构要求已对齐：CNN 输出 6 维、DeepSphere 输入 9 维。  
